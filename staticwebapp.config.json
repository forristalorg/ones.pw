{   
    "networking":{
        "forwardingGateway": {
            "allowedForwardedHosts": [
            "ones.pw",
            "dev.ones.pw"
            ],
            "requiredHeaders": {
                "X-CDN-TOKEN" : " ${{ secrets.HEADER_TOKEN }}"
            }
        }
    }
}