name: Azure Static Web Apps CI/CD

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
      - main

jobs:
  
  build_and_deploy_job:
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    name: Build and Deploy Job
    steps:
      - uses: actions/checkout@v2
        with:
          submodules: true
      - name: Secret Parser
        # You may pin to the exact commit or the version.
        # uses: jwsi/secret-parser@2c174fe999761cebeb83f6c6d79dfc938e3e473e
        uses: jwsi/secret-parser@v1
        with:
          # File where GitHub Actions Secrets references should be parsed and replaced
          filename: staticwebapp.config.json
          # Name of secret to search for in the designated file
          secret-name: HEADER_TOKEN
          # Value of secret to replace reference with in designated file
          secret-value:  ${{ secrets.HEADER_TOKEN }} 
      - name: Secret Parser - index.html
        # You may pin to the exact commit or the version.
        # uses: jwsi/secret-parser@2c174fe999761cebeb83f6c6d79dfc938e3e473e
        uses: jwsi/secret-parser@v1
        with:
          # File where GitHub Actions Secrets references should be parsed and replaced
          filename: index.html
          # Name of secret to search for in the designated file
          secret-name: Canary
          # Value of secret to replace reference with in designated file
          secret-value:  ${{ secrets.Canary }}           
      - name: Build And Deploy
        id: builddeploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_AGREEABLE_WATER_08EBBEC10 }}
          repo_token: ${{ secrets.GITHUB_TOKEN }} # Used for Github integrations (i.e. PR comments)
          action: "upload"
          ###### Repository/Build Configurations - These values can be configured to match your app requirements. ######
          # For more information regarding Static Web App workflow configurations, please visit: https://aka.ms/swaworkflowconfig
          app_location: "/" # App source code path
          api_location: "" # Api source code path - optional
          output_location: "" # Built app content directory - optional
          ###### End of Repository/Build Configurations ######

  close_pull_request_job:
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    name: Close Pull Request Job
    steps:
      - name: Close Pull Request
        id: closepullrequest
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_AGREEABLE_WATER_08EBBEC10 }}
          action: "close"
